/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {OaiHarvestJobStatistics} from './oai-harvest-job-statistics';

describe('OaiHarvestJobStatistics', () => {
  it('should create an instance', () => {
    expect(new OaiHarvestJobStatistics()).toBeTruthy();
  });
});
